# Prior-art review packet under the replacement plan — executed 2026-09-15 (URC-R03)

Plan: [docs/specs/prior-art-closeout/plan.md](../../docs/specs/prior-art-closeout/plan.md) (PR #13, merged as `07c037d`). Starting state (T00): main `dc6a39f` already contained PR #12 (the original plan) and PR #14 (its implementation); this packet reconciles those outputs under the replacement plan rather than starting from the reviewed base `2c72e02`. Branch `task/prior-art-closeout-2026-09-15`. Interpreter `/Users/redhose/ENTER/bin/python3` 3.11.8, jsonschema 4.26.0, pdftotext 26.08.0. Input hashes, routes and per-source snapshots: [sources.json](sources.json) (snapshots outside git under `~/.cache/uav-failsafe-composition/prior-art-closeout/2026-09-15/`).

## What was done, task by task

| task | outcome |
|---|---|
| T00 | Starting state recorded here; baseline at start: 40 repository tests, 4 presentation tests, contract PASS. ID collisions checked (no prior S/C ids; URC-R03 row from #14 rewritten). |
| T01 | Rubric clarification committed in `docs/day3-reading-rubric.md` (axis definitions, access vocabulary, identity map, acquisition limit, relevance screen, conclusion rule); canonical export hash frozen in `sources.json`. Targeted follow-up, not a blind preregistration. |
| T02 | Regressions added to `tests/test_acquisition_ledger.py` (AccessPromotionTests) and `tests/test_reference_coverage.py` (uninspected access, missing axes, real-axis blank/unknown states, inspected-with-locator). Before the fixes they failed as intended: [t02-before-fix.txt](t02-before-fix.txt) (5 failures, 7 errors). |
| T03 | `build()` promotes only the inspected-access allowlist with a non-empty locator; `novelty_axes()` scores the four canonical axes for every record and treats unread access, empty locator, missing or invalid state as unresolved. No new module or dependency. |
| T04 | S1/S2 identities by Crossref DOI; S3 = first of five Crossref bibliographic results, title and five authors match, **IEEE S&P 2022** (the inherited "2026" label was wrong); publisher copy closed, author-posted PDF read. Routes, statuses and hashes in `sources.json`. |
| T05–T07 | S1–S3 records: all four axes `not_found_in_inspected`, each axis text names its section. |
| T08–T09 | U3 pinned at commit `7eaebf21116087249b8329d4ba7337a24a34ecb9`; seven files opened and hashed (`files_inspected`). Reuse decision below. |
| T10 | U1–U12 reconciled from re-opened locators: **18 judgments changed** ([decision-changes.json](decision-changes.json)); U10's `not_applicable` judgments retained on re-read; U9's 3-second sentence not found in today's page text, judgment retained on its 2026-09-12 retrieval and flagged. |
| T11 | All **317** canonical rows screened from metadata ([candidate-screening.csv](candidate-screening.csv)): 4 linked to inspected records, 1 duplicate alias, 7 `needs_full_text`, 10 `unresolved_metadata`, 295 excluded with a named criterion. 17 metadata-only C-records added, all axes unresolved. |
| T12 | Both derived JSON artifacts regenerated by script; second regeneration byte-identical; 402 historical rows and 90 provenance gaps unchanged; canonical `day4_public`, coverage 3/6, rejected 0/6; register byte-identical. Coverage doc: superseded passages marked, reconciled section added. |
| T13 | `docs/prior-art.md` URC-01 decision: search history by kind (web-index incl. three patent-domain strings; native exports; identity lookups; U12 claims; not-performed searches stated), criteria, comparator table, evidence conclusion per axis, **gate status partial**. |
| T14 | `URC-R03` ledger row rewritten; progress and review entries; START_HERE and README pointers; earlier same-day evidence marked superseded. |
| T15 | Acceptance tests (unique ids, mandatory U1–U12/S1–S3, four valid axes per record, unread ⇒ unresolved, full screening accounting). Controls below. |
| T16 | This PR; CI on its head reported in the PR. |

## Result

Reconciled axis table: `equivalent_intent` and `coverage` **supported_bounded** with 17 unresolved C-ids (no inspected source discloses either); `reconnection` narrowed by documentation (U8, U12); `liveness_vs_setpoint_injection` narrowed by documentation (U7, U9). Evidence conclusion: prior art found on the two documented axes; **no conclusion** on the other two while relevant intake rows are unread. URC-01 gate: **partial**. No owner gate closed.

## U3 reuse decision (T09)

Do not reuse the code: Python 2, pymavlink-bound monolithic scripts (2.4k and 2.7k lines) with hard-coded per-autopilot policy lists and a simulator-reboot crash oracle; untested compatibility with current SITL. Reuse the *form* of its RC/GPS fail-safe predicates (state predicates over telemetry and parameters with a signed distance) as a reference when writing recovery-contract predicates; oracles and distance guidance are not a coverage endpoint. This relates to the U5/U6 tooling options already reviewed (a shared ROS 2 autopilot interface; PX4's failsafe simulator) without implementing a launcher.

## Checks observed (final head of this branch)

| command | observed |
|---|---|
| `python -m unittest discover -s tests -q` | Ran 52 tests, OK (12 new) |
| `python scripts/check_repo_contract.py` | PASS |
| `python scripts/acquisition_ledger.py --check` | consistent |
| `python scripts/reference_coverage.py --check` | OK, day2 0/6, day4 3/6 |
| `python evidence/task-2026-09-09/rerun_search.py --audit evidence/task-2026-09-11-public/database-export.json` | exit 0 |
| `python tools/check_presentation.py . "UAV Failsafe Composition" uav-failsafe-composition` | no issues |
| `python tools/test_presentation.py` | OK (4) |
| `git diff --check` | clean |
| regenerate both artifacts twice | byte-identical (`837fed70…`, `a0e775a7…` prefixes) |

## Negative controls (disposable copy, restored afterwards)

| control | observed |
|---|---|
| remove S1, leave generated JSON | `reference_coverage --check` exit 1; `acquisition_ledger --check` exit 1 |
| remove S1, then regenerate | both `--check` pass (stale-output tests alone cannot see the omission) and `test_ids_unique_and_mandatory_sources_present` **fails** — regenerated omissions cannot look complete |
| unsafe access promotion / missing axes | fail on the pre-fix consumers ([t02-before-fix.txt](t02-before-fix.txt)); pass after T03 |
| invented identifier in an export | existing tamper test retained (`test_identifier_absent_from_the_raw_response_is_not_credited`) |

Software checks establish consistency, not correct paper interpretation. Executor self-review: every changed judgment in `decision-changes.json` was compared with its retained locator in the cached snapshot before commit.

## Not done / next bounded work

Full-text reading of C001, C087, C158, C180, C186, C214, C228; resolution of the 10 `unresolved_metadata` rows; U9 re-verification; native patent, dissertation and standards searches (owner decision, XC-02). No simulator, no new search legs, no candidate.json re-pin, no PR #6 or rename housekeeping.
