# URC-R02 — reference-coverage reconciliation and closest-competitor review — 2026-09-12

Branch `audit/reference-coverage-20260912` off `main` c1519cf. Deliverable: [docs/reference-coverage-2026-09-12.md](../../docs/reference-coverage-2026-09-12.md).

## What was done
- `docs/source-eligibility-register.json`: all 12 day-1 sources, identifiers resolved from primary metadata (OpenAlex by DOI, Crossref bibliographic, arXiv HTML header), explicit eligibility decision each.
- `scripts/reference_coverage.py`: recall over eligible sources against both exports under all identifier aliases; `--check` mode; output `reference-coverage.json` here.
- `docs/day3-reading-records.json`: 7 records added (U2, U3, U4, U6, U9, U10, U11), U7/U8 re-inspected with exact locators; every record carries a sha256 prefix of the fetched text (`fetched-text-hashes.json`).
- `evidence/task-day3-2026-09-09/acquisition-ledger.json` regenerated by its own script because it derives from the reading records.
- Ledger: URC-R02 → done. URC-D02 untouched (stays as the day-4 review left it).

## Finding
Both earlier recall figures undercounted: anchors were arXiv-only, so PGFuzz (NDSS DOI) and the published RouthSearch (ACM DOI) were retrieved by both exports and never credited. Historical 0/6 → **2/6**; public 2/5 → **3/6** (same denominator now). The historical export's provenance defect is unchanged.

## Checks observed (repo root on PYTHONPATH, as the test modules themselves import `scripts.`)
| command | observed |
|---|---|
| `PYTHONPATH=. python -m unittest discover -s tests` | Ran 36 tests, OK (6 new) |
| `python scripts/check_repo_contract.py` | PASS |
| `python scripts/reference_coverage.py --check` | OK, 2/6 and 3/6 |
| `python scripts/acquisition_ledger.py --check` | consistent |
| negative control: remove U11 from the register | 2 failures in test_reference_coverage, as required |

## Not done
No source screened beyond the day-1 set; ADGFuzz, UAVConfigFuzzer, PGPatch surfaced by Crossref are UNSCREENED. No novelty, patent or owner gate closed. arXiv API returned 429 all day; not used. github.com (U3) not re-read.

## Review repair (URC-R02b, same day)
- `scripts/reference_coverage.py` is provenance-bound: hits without a logged successful query are excluded from credit and enumerated; canonical export = `day4_public`, rejected = `['day2_historical']`. Recall unchanged (no anchor was untraceable); the record now says what it can show.
- Novelty-axis table generated from the reading records: each axis is `narrowed_by_disclosure` / `supported_bounded` / `unresolved`, with the sources in each state named. Abstract-only and inaccessible sources never count as support.
- `URC-D02` reconciled and closed against this single record; no successor row.
